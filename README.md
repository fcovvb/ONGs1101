Crear Máquina Virtual || Acá creamos BD
	generar clave aleatoria public y private en putty
	conectarse a la maquina virtual
	Crear password ubuntu || sudo passwd azureuser
	Instalar xfce || Entorno de escritorio ||
		sudo apt-get update
		sudo apt-get install xfce4  || Creamos App Service

	Instalar Remote Desktop || sudo apt-get install xrdp
				   echo xfce4-session
				   sudo service xrdp restart
	Abrir puertos de la red 3389
	Instalar Browser Chromium || sudo apt-get install chromium-browser

Crear App Service || Wordpress


Crear Bases de Datos MySQL como servicio en Azure
	Agregar IP al firewall de la base de datos

		t-frvale@microsoft.com

# ONGs1101
